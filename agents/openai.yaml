interface:
  display_name: "Short Drama Production Control"
  short_description: "Manage short-drama dependencies, approvals, H3 jobs, and QC with 16:9 as the default aspect ratio"
  default_prompt: "Use $short-drama-production to inspect this project's status and dependencies, and advance to the next stage only within the scope I have authorized."
